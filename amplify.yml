version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build required for static files"
    build:
      commands:
        - echo "Build complete - static files ready"
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths:
      - public/**/*
